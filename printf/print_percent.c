#include <unistd.h>
#include "main.h"
/**
 * print_percent: prints the % character to stdout
 * @args: variadic parameter
 *
 * Return number of characters printed
 */

int print_percent(va_list print)
{
        
        return(0);
}
